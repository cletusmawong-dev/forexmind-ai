# Template: copy to vps-bridge\.env on the VPS and fill in, or let
# install.ps1 set machine-level env vars (preferred - covers Task Scheduler).
BRIDGE_TOKEN=paste-a-long-random-secret-here
BRIDGE_PORT=8700
